# Development cases (inspectable)

> **Status:** EMPTY / PLACEHOLDER. **6–8** development cases go here. Not yet authored — do **not**
> implement case contents in the skeleton PR. See `../../README.md` for the frozen design.

Purpose: build, debug, and calibrate the harness and rubric. Freely inspectable.

Each case (TODO — define the exact on-disk schema) will contain:
- a **fixed frozen evidence pack** (the only information any arm may use — no live research);
- case metadata (scarce capital frame, decision to be made);
- a **sealed answer key** encoding the objective-metric ground truth (GO/KILL/DEFER, kill/no-kill,
  cheapest decisive action, capital-within-frame, decisive assumptions).

Coverage intent (author across these archetypes): a genuinely attractive opportunity, a
seductive-but-weak opportunity, and an obviously weak opportunity — under contradictory/incomplete
evidence.
