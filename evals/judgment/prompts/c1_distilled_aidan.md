# Arm C1 — Distilled-AIDAN prompt (control / simplicity kill test)

> **Status:** PLACEHOLDER. Not yet authored. See `../README.md` for the frozen design.

**Role in the experiment:** the **pivotal** control and the **simplicity kill test**. A single
compact prompt that distills AIDAN's *doctrine* into one pass — **without** the full staged
multi-step process of arm T. Same model, same frozen evidence pack.

If C1 captures nearly all of T's cost-adjusted benefit, AIDAN's staged structure is **not justified**
and must be simplified (see `../README.md` → Kill rules).

TODO — author the prompt. It must, in **one pass**, encode the AIDAN doctrine only:
- Highest-value **next action**, not "build" by default.
- Kill Case first; weak ventures should die cheaply (capital scarcity, ADR-037).
- Evidence discipline: distinguish OBSERVED / SOURCE-CONFIRMED / STRATEGIC-JUDGEMENT / UNVERIFIED;
  no fabricated evidence.
- Cheapest decisive action; spend within the scarce frame.
- Emit the same machine-readable output schema as C0 and T (schema TODO).
- Deliberately **omit** the staged decomposition (that is T's differentiator).
