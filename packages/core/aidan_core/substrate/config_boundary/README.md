# CONFIG_BOUNDARY (Venture Substrate component)

Reusable **infrastructure** convention for environment configuration and secret
exclusion. It is materialized identically into every venture workspace that
selects it. It defines *how* configuration is read and *which files must never be
committed* — never a product concept, workflow, screen, or brand.
