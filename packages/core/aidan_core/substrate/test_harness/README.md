# TEST_HARNESS (Venture Substrate component)

Reusable **infrastructure** convention for how a venture declares and runs its
tests, and the deterministic machine rule for success (exit code zero). It is
materialized identically into every venture workspace that selects it. It defines
*how tests are invoked*, never *what the product is*.
